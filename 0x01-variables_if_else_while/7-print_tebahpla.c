#include <stdio.h>

/**
* main - prints the lowercase alphabet in reverse,
*
* Return: Always 0 (Success)
*/

int main(void)

{
	char alpha;

	int i;

	for (i = 0; i < 26; i--)
	{
		putchar(alpha);
	}
	putchar('\n');
	return (0);
}
